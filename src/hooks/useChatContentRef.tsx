import { ForwardedRef } from "react";

export default function useChatContentRef(ref: ForwardedRef<any>) {
  // function scrollToBottom() {
  //   let container = contentRef.current;
  //   debugger;
  //   // container.scrollTop = container.scrollHeight;
  // }
  // return {
  //   contentRef: ref,
  //   scrollToBottom,
  // };
}
